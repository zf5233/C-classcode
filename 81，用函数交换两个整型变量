#include <stdio.h>
void swap(int x, int y)
{
	int tmp = 0;
	tmp = x;
	x = y;
	y = tmp;
}
int main()
{
	int a = 10;
	int b = 20;
	int tmp = 0;
	printf("a = %d b = %d\n", a, b);//这种方法是错误的，不能交换。只是令x = 20 y =10
	swap(a, b);
	printf("a = %d b = %d\n", a, b);
	return 0;
}
