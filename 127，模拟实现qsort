#include<stdio.h>
#include<string.h>
//模拟实现qsort,实现冒泡排序
//不同类型的数据排序方法不同
//1,首先考虑，第一个参数能接收任意元素的地址
//
struct Stu//必须写在cmp_struct前面，否则运行失败
{
	char name[20];
	int age;
};
void bubble_sort(void* base,int sz,int width,int(*cmp)())
{
	int i = 0;//趟数
	for (i = 0; i < sz - 1; i++)//每一趟比较的对数，有多少对需要比较
	{
		int j = 0;
		for (j=0;j<sz-i;j++)//2个元素的比较
		{
			if()

		}
	}
}
void test1()
{
	int arr[10] = { 9,8,7,6,4,5,3,1,2,0 };
	int sz = sizeof(arr) / sizeof(arr[0]);
	
}
void test2()
{
	float f[] = { 9.0,8.0,7.0,5.0,6.0 };
	
}
void test3()
{
	struct Stu s[2] = { {"zhangsan",40},{"lisi",30} };
	int sz = sizeof(s) / sizeof(s[0]);
	
}
int main()
{
	test1();
	test2();
	test3();
	return 0;
}
