#include <stdio.h>
#include <string.h>

int main()
{
	int arr[10] = {0};//10个整型元素的数组
	int sz = 0;
	printf("%d\n", sizeof(arr));//40
	sz = sizeof(arr)/sizeof(arr[0]);//计算数组的元素个数=数组总大小/每个元素的大小
	printf("sz = %d\n", sz);//sz = 10
	return 0;
}
