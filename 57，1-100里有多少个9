#include <stdio.h>
int main()
{
	int i = 0;
	int count = 0;
	for (i = 1; i <= 100; i++)
	{
		if (i % 10 == 9)
			count++;
		if (i / 10 == 9)//两个if为并列关系，如果为else if则是19个
			count++;
	}
	printf("count = %d\n", count);
	return 0;
}
